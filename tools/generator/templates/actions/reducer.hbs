import {

} from 'constants/actionTypes';
import initialState from './initialState';

export default function {{camelCase reducerName}}(state = initialState.{{camelCase reducerName}}, action) {
  switch (action.type) {

    default:
      return state;
  }
}
